// String, number, undefined, null, boolean, symbol
// NaN - Not a number
// typeof - verifica o tipo de elemento utilizado no código.

const name = "Ivan"; //string
const num = 28; //number
let nomeMateria; //undefined -> não aponta para local nenhum na memória
const sobrenome = null; //null -> não aponta para local nenhum na memória
const certificado = true; //Boolean = true, false (lógico)
