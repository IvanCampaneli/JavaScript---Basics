// (quando chama parentese, se chama uma função e uma função executa uma ação)
// código clean `crase dentro de todo o parensete e função dentro de ${template}`
// alert(caixa de mensagem) - prompt(formatar a caixa de msg para text) - confirm(atribuir o valor)

let num1 = prompt('Digite um número');
let num2 = prompt('Digite outro número');

num1 = Number(num1);
num2 = Number(num2);

alert (`Resultado:  ${num1 + num2}`);

